# Climate time series API
#
# The API allows the public to search and consult time series about climate change research conducted by TheShiftProject.  The climate time series API allows contributors to upload time series about their climate change models and studies.  Other secured endpoints allows admins to maintain the nomenclatures used by the climate time series, such as units etc. Timeseries publication status and ownership is for now managed by admins. 
#
# OpenAPI spec version: v0.0.1
# Contact: fredbi@yahoo.com
# Generated by: https://github.com/swagger-api/swagger-codegen.git

#' @title Classes operations
#' @description swagger.Classes
#'
#' @field path Stores url path of the request.
#' @field apiClient Handles the client-server communication.
#' @field userAgent Set the user agent of the request.
#'
#' @importFrom R6 R6Class
#'
#' @section Methods:
#' \describe{
#'
#' classes_class_id_get Get metadata about a nomenclature class
#'
#'
#' classes_class_id_members_class_member_id_delete Removes a member from a nomenclature class
#'
#'
#' classes_class_id_members_class_member_id_put Update a member of a nomenclature class
#'
#'
#' classes_class_id_members_get Get all the members of a nomenclature class
#'
#'
#' classes_class_id_members_post Add a new member in a nomenclature class
#'
#'
#' classes_class_id_put Update descriptive metadata about a nomenclature class
#'
#'
#' classes_get Get all valid nomenclature classes
#'
#' }
#'
#' @export
ClassesApi <- R6::R6Class(
  'ClassesApi',
  public = list(
    userAgent = "Swagger-Codegen/1.0.0/r",
    apiClient = NULL,
    initialize = function(apiClient){
      if (!missing(apiClient)) {
        self$apiClient <- apiClient
      }
      else {
        self$apiClient <- ApiClient$new()
      }
    },
    classes_class_id_get = function(class_id, deep, brief, audit, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`deep`)) {
        queryParams['deep'] <- deep
      }

      if (!missing(`brief`)) {
        queryParams['brief'] <- brief
      }

      if (!missing(`audit`)) {
        queryParams['audit'] <- audit
      }

      urlPath <- "/classes/{classId}"
      if (!missing(`class_id`)) {
        urlPath <- gsub(paste0("\\{", "classId", "\\}"), `class_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- ClassDescription$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    classes_class_id_members_class_member_id_delete = function(class_id, class_member_id, class_member, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`class_member`)) {
        body <- `class_member`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/classes/{classId}/members/{classMemberId}"
      if (!missing(`class_id`)) {
        urlPath <- gsub(paste0("\\{", "classId", "\\}"), `class_id`, urlPath)
      }

      if (!missing(`class_member_id`)) {
        urlPath <- gsub(paste0("\\{", "classMemberId", "\\}"), `class_member_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    classes_class_id_members_class_member_id_put = function(class_id, class_member_id, class_member, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`class_member`)) {
        body <- `class_member`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/classes/{classId}/members/{classMemberId}"
      if (!missing(`class_id`)) {
        urlPath <- gsub(paste0("\\{", "classId", "\\}"), `class_id`, urlPath)
      }

      if (!missing(`class_member_id`)) {
        urlPath <- gsub(paste0("\\{", "classMemberId", "\\}"), `class_member_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PUT",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    classes_class_id_members_get = function(class_id, glob, deep, brief, audit, tag, tags, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`glob`)) {
        queryParams['glob'] <- glob
      }

      if (!missing(`deep`)) {
        queryParams['deep'] <- deep
      }

      if (!missing(`brief`)) {
        queryParams['brief'] <- brief
      }

      if (!missing(`audit`)) {
        queryParams['audit'] <- audit
      }

      if (!missing(`tag`)) {
        queryParams['tag'] <- tag
      }

      if (!missing(`tags`)) {
        queryParams['tags'] <- tags
      }

      urlPath <- "/classes/{classId}/members"
      if (!missing(`class_id`)) {
        urlPath <- gsub(paste0("\\{", "classId", "\\}"), `class_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- ClassNomenclature$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    classes_class_id_members_post = function(class_id, class_member, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`class_member`)) {
        body <- `class_member`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/classes/{classId}/members"
      if (!missing(`class_id`)) {
        urlPath <- gsub(paste0("\\{", "classId", "\\}"), `class_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    classes_class_id_put = function(class_id, class_description, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`class_description`)) {
        body <- `class_description`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/classes/{classId}"
      if (!missing(`class_id`)) {
        urlPath <- gsub(paste0("\\{", "classId", "\\}"), `class_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PUT",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- ClassDescription$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    classes_get = function(glob, deep, brief, audit, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`glob`)) {
        queryParams['glob'] <- glob
      }

      if (!missing(`deep`)) {
        queryParams['deep'] <- deep
      }

      if (!missing(`brief`)) {
        queryParams['brief'] <- brief
      }

      if (!missing(`audit`)) {
        queryParams['audit'] <- audit
      }

      urlPath <- "/classes"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- ClassNomenclature$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    }
  )
)
