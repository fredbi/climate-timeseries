# Climate time series API
#
# The API allows the public to search and consult time series about climate change research conducted by TheShiftProject.  The climate time series API allows contributors to upload time series about their climate change models and studies.  Other secured endpoints allows admins to maintain the nomenclatures used by the climate time series, such as units etc. Timeseries publication status and ownership is for now managed by admins. 
#
# OpenAPI spec version: v0.0.1
# Contact: fredbi@yahoo.com
# Generated by: https://github.com/swagger-api/swagger-codegen.git

#' @title Series operations
#' @description swagger.Series
#'
#' @field path Stores url path of the request.
#' @field apiClient Handles the client-server communication.
#' @field userAgent Set the user agent of the request.
#'
#' @importFrom R6 R6Class
#'
#' @section Methods:
#' \describe{
#'
#' series_get Get all available series, with search filters
#'
#'
#' series_post Creates a new time series
#'
#'
#' series_series_id_delete Deletes a time series
#'
#'
#' series_series_id_get Get all versions of a time series.
#'
#'
#' series_series_id_latest_get Get the latest version of the description of a time series
#'
#'
#' series_series_id_latest_values_get Get the values from the latest version of a time series
#'
#'
#' series_series_id_post Creates a new version of a time series
#'
#'
#' series_series_id_put Updates metadata about a time series
#'
#'
#' series_series_id_semver_get Get all semver tags associated to a series
#'
#'
#' series_series_id_semver_semver_get Get a version of a time series with a semver tag
#'
#'
#' series_series_id_semver_semver_values_get Get the values of version of a time series with a semver tag
#'
#'
#' series_versions_versioned_series_id_delete Deletes a version of a time series
#'
#'
#' series_versions_versioned_series_id_get Get a version of a time series
#'
#'
#' series_versions_versioned_series_id_post creates a new version of a time series
#'
#'
#' series_versions_versioned_series_id_put Updates the metadata of version of a time series
#'
#'
#' series_versions_versioned_series_id_values_delete Deletes the values attached to a version of a time series
#'
#'
#' series_versions_versioned_series_id_values_get Get the values of version of a time series
#'
#'
#' series_versions_versioned_series_id_values_post Uploads values to the version of a time series
#'
#'
#' series_versions_versioned_series_id_values_put Replaces the values of version of a time series
#'
#' }
#'
#' @export
SeriesApi <- R6::R6Class(
  'SeriesApi',
  public = list(
    userAgent = "Swagger-Codegen/1.0.0/r",
    apiClient = NULL,
    initialize = function(apiClient){
      if (!missing(apiClient)) {
        self$apiClient <- apiClient
      }
      else {
        self$apiClient <- ApiClient$new()
      }
    },
    series_get = function(glob, tag, tags, theme, zone, status, owner_id, datasource, email, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`glob`)) {
        queryParams['glob'] <- glob
      }

      if (!missing(`tag`)) {
        queryParams['tag'] <- tag
      }

      if (!missing(`tags`)) {
        queryParams['tags'] <- tags
      }

      if (!missing(`theme`)) {
        queryParams['theme'] <- theme
      }

      if (!missing(`zone`)) {
        queryParams['zone'] <- zone
      }

      if (!missing(`status`)) {
        queryParams['status'] <- status
      }

      if (!missing(`owner_id`)) {
        queryParams['ownerId'] <- owner_id
      }

      if (!missing(`datasource`)) {
        queryParams['datasource'] <- datasource
      }

      if (!missing(`email`)) {
        queryParams['email'] <- email
      }

      urlPath <- "/series"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Series$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    series_post = function(series, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`series`)) {
        body <- `series`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/series"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    series_series_id_delete = function(series_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/series/{seriesId}"
      if (!missing(`series_id`)) {
        urlPath <- gsub(paste0("\\{", "seriesId", "\\}"), `series_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    series_series_id_get = function(series_id, glob, deep, brief, audit, status, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`glob`)) {
        queryParams['glob'] <- glob
      }

      if (!missing(`deep`)) {
        queryParams['deep'] <- deep
      }

      if (!missing(`brief`)) {
        queryParams['brief'] <- brief
      }

      if (!missing(`audit`)) {
        queryParams['audit'] <- audit
      }

      if (!missing(`status`)) {
        queryParams['status'] <- status
      }

      urlPath <- "/series/{seriesId}"
      if (!missing(`series_id`)) {
        urlPath <- gsub(paste0("\\{", "seriesId", "\\}"), `series_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- VersionedSeries$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    series_series_id_latest_get = function(series_id, deep, brief, audit, status, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`deep`)) {
        queryParams['deep'] <- deep
      }

      if (!missing(`brief`)) {
        queryParams['brief'] <- brief
      }

      if (!missing(`audit`)) {
        queryParams['audit'] <- audit
      }

      if (!missing(`status`)) {
        queryParams['status'] <- status
      }

      urlPath <- "/series/{seriesId}/latest"
      if (!missing(`series_id`)) {
        urlPath <- gsub(paste0("\\{", "seriesId", "\\}"), `series_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- VersionedSeries$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    series_series_id_latest_values_get = function(series_id, from, to, convert, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`from`)) {
        queryParams['from'] <- from
      }

      if (!missing(`to`)) {
        queryParams['to'] <- to
      }

      if (!missing(`convert`)) {
        queryParams['convert'] <- convert
      }

      urlPath <- "/series/{seriesId}/latest/values"
      if (!missing(`series_id`)) {
        urlPath <- gsub(paste0("\\{", "seriesId", "\\}"), `series_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- TsValues$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    series_series_id_post = function(series_id, series, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`series`)) {
        body <- `series`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/series/{seriesId}"
      if (!missing(`series_id`)) {
        urlPath <- gsub(paste0("\\{", "seriesId", "\\}"), `series_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    series_series_id_put = function(series_id, series, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`series`)) {
        body <- `series`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/series/{seriesId}"
      if (!missing(`series_id`)) {
        urlPath <- gsub(paste0("\\{", "seriesId", "\\}"), `series_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PUT",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    series_series_id_semver_get = function(series_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/series/{seriesId}/semver"
      if (!missing(`series_id`)) {
        urlPath <- gsub(paste0("\\{", "seriesId", "\\}"), `series_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Character$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    series_series_id_semver_semver_get = function(series_id, semver, deep, brief, audit, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`deep`)) {
        queryParams['deep'] <- deep
      }

      if (!missing(`brief`)) {
        queryParams['brief'] <- brief
      }

      if (!missing(`audit`)) {
        queryParams['audit'] <- audit
      }

      urlPath <- "/series/{seriesId}/semver/{semver}"
      if (!missing(`series_id`)) {
        urlPath <- gsub(paste0("\\{", "seriesId", "\\}"), `series_id`, urlPath)
      }

      if (!missing(`semver`)) {
        urlPath <- gsub(paste0("\\{", "semver", "\\}"), `semver`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- VersionedSeries$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    series_series_id_semver_semver_values_get = function(series_id, semver, from, to, convert, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`from`)) {
        queryParams['from'] <- from
      }

      if (!missing(`to`)) {
        queryParams['to'] <- to
      }

      if (!missing(`convert`)) {
        queryParams['convert'] <- convert
      }

      urlPath <- "/series/{seriesId}/semver/{semver}/values"
      if (!missing(`series_id`)) {
        urlPath <- gsub(paste0("\\{", "seriesId", "\\}"), `series_id`, urlPath)
      }

      if (!missing(`semver`)) {
        urlPath <- gsub(paste0("\\{", "semver", "\\}"), `semver`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- TsValues$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    series_versions_versioned_series_id_delete = function(versioned_series_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/series/versions/{versionedSeriesId}"
      if (!missing(`versioned_series_id`)) {
        urlPath <- gsub(paste0("\\{", "versionedSeriesId", "\\}"), `versioned_series_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    series_versions_versioned_series_id_get = function(versioned_series_id, deep, brief, audit, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`deep`)) {
        queryParams['deep'] <- deep
      }

      if (!missing(`brief`)) {
        queryParams['brief'] <- brief
      }

      if (!missing(`audit`)) {
        queryParams['audit'] <- audit
      }

      urlPath <- "/series/versions/{versionedSeriesId}"
      if (!missing(`versioned_series_id`)) {
        urlPath <- gsub(paste0("\\{", "versionedSeriesId", "\\}"), `versioned_series_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- VersionedSeries$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    series_versions_versioned_series_id_post = function(versioned_series_id, version_series, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`version_series`)) {
        body <- `version_series`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/series/versions/{versionedSeriesId}"
      if (!missing(`versioned_series_id`)) {
        urlPath <- gsub(paste0("\\{", "versionedSeriesId", "\\}"), `versioned_series_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    series_versions_versioned_series_id_put = function(versioned_series_id, version_series, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`version_series`)) {
        body <- `version_series`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/series/versions/{versionedSeriesId}"
      if (!missing(`versioned_series_id`)) {
        urlPath <- gsub(paste0("\\{", "versionedSeriesId", "\\}"), `versioned_series_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PUT",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    series_versions_versioned_series_id_values_delete = function(versioned_series_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/series/versions/{versionedSeriesId}/values"
      if (!missing(`versioned_series_id`)) {
        urlPath <- gsub(paste0("\\{", "versionedSeriesId", "\\}"), `versioned_series_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    series_versions_versioned_series_id_values_get = function(versioned_series_id, from, to, convert, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`from`)) {
        queryParams['from'] <- from
      }

      if (!missing(`to`)) {
        queryParams['to'] <- to
      }

      if (!missing(`convert`)) {
        queryParams['convert'] <- convert
      }

      urlPath <- "/series/versions/{versionedSeriesId}/values"
      if (!missing(`versioned_series_id`)) {
        urlPath <- gsub(paste0("\\{", "versionedSeriesId", "\\}"), `versioned_series_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- TsValues$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    series_versions_versioned_series_id_values_post = function(versioned_series_id, values, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`values`)) {
        body <- `values`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/series/versions/{versionedSeriesId}/values"
      if (!missing(`versioned_series_id`)) {
        urlPath <- gsub(paste0("\\{", "versionedSeriesId", "\\}"), `versioned_series_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    series_versions_versioned_series_id_values_put = function(versioned_series_id, values, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`values`)) {
        body <- `values`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/series/versions/{versionedSeriesId}/values"
      if (!missing(`versioned_series_id`)) {
        urlPath <- gsub(paste0("\\{", "versionedSeriesId", "\\}"), `versioned_series_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PUT",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    }
  )
)
