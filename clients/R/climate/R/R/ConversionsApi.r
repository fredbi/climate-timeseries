# Climate time series API
#
# The API allows the public to search and consult time series about climate change research conducted by TheShiftProject.  The climate time series API allows contributors to upload time series about their climate change models and studies.  Other secured endpoints allows admins to maintain the nomenclatures used by the climate time series, such as units etc. Timeseries publication status and ownership is for now managed by admins. 
#
# OpenAPI spec version: v0.0.1
# Contact: fredbi@yahoo.com
# Generated by: https://github.com/swagger-api/swagger-codegen.git

#' @title Conversions operations
#' @description swagger.Conversions
#'
#' @field path Stores url path of the request.
#' @field apiClient Handles the client-server communication.
#' @field userAgent Set the user agent of the request.
#'
#' @importFrom R6 R6Class
#'
#' @section Methods:
#' \describe{
#'
#' conversion_from_unit_to_unit_get Get the conversion specification from unit to unit.
#'
#'
#' conversion_get Get the conversion specification from unit to unit.
#'
#'
#' conversions_from_unit_to_unit_delete Delete an existing conversion specification from unit to unit.
#'
#'
#' conversions_from_unit_to_unit_post Creates add new conversion specification from unit to unit.
#'
#'
#' conversions_from_unit_to_unit_put Update an existing conversion specification from unit to unit.
#'
#'
#' conversions_get List available unit conversions, with some query filters
#'
#' }
#'
#' @export
ConversionsApi <- R6::R6Class(
  'ConversionsApi',
  public = list(
    userAgent = "Swagger-Codegen/1.0.0/r",
    apiClient = NULL,
    initialize = function(apiClient){
      if (!missing(apiClient)) {
        self$apiClient <- apiClient
      }
      else {
        self$apiClient <- ApiClient$new()
      }
    },
    conversion_from_unit_to_unit_get = function(from_unit, to_unit, deep, brief, audit, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`deep`)) {
        queryParams['deep'] <- deep
      }

      if (!missing(`brief`)) {
        queryParams['brief'] <- brief
      }

      if (!missing(`audit`)) {
        queryParams['audit'] <- audit
      }

      urlPath <- "/conversion/{fromUnit}/{toUnit}"
      if (!missing(`from_unit`)) {
        urlPath <- gsub(paste0("\\{", "fromUnit", "\\}"), `from_unit`, urlPath)
      }

      if (!missing(`to_unit`)) {
        urlPath <- gsub(paste0("\\{", "toUnit", "\\}"), `to_unit`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- ConversionSpec$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    conversion_get = function(from_unit, to_unit, deep, brief, audit, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`from_unit`)) {
        queryParams['fromUnit'] <- from_unit
      }

      if (!missing(`to_unit`)) {
        queryParams['toUnit'] <- to_unit
      }

      if (!missing(`deep`)) {
        queryParams['deep'] <- deep
      }

      if (!missing(`brief`)) {
        queryParams['brief'] <- brief
      }

      if (!missing(`audit`)) {
        queryParams['audit'] <- audit
      }

      urlPath <- "/conversion"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Conversion$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    conversions_from_unit_to_unit_delete = function(from_unit, to_unit, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/conversions/{fromUnit}/{toUnit}"
      if (!missing(`from_unit`)) {
        urlPath <- gsub(paste0("\\{", "fromUnit", "\\}"), `from_unit`, urlPath)
      }

      if (!missing(`to_unit`)) {
        urlPath <- gsub(paste0("\\{", "toUnit", "\\}"), `to_unit`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    conversions_from_unit_to_unit_post = function(from_unit, to_unit, conversion_spec, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`conversion_spec`)) {
        body <- `conversion_spec`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/conversions/{fromUnit}/{toUnit}"
      if (!missing(`from_unit`)) {
        urlPath <- gsub(paste0("\\{", "fromUnit", "\\}"), `from_unit`, urlPath)
      }

      if (!missing(`to_unit`)) {
        urlPath <- gsub(paste0("\\{", "toUnit", "\\}"), `to_unit`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    conversions_from_unit_to_unit_put = function(from_unit, to_unit, conversion_spec, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`conversion_spec`)) {
        body <- `conversion_spec`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/conversions/{fromUnit}/{toUnit}"
      if (!missing(`from_unit`)) {
        urlPath <- gsub(paste0("\\{", "fromUnit", "\\}"), `from_unit`, urlPath)
      }

      if (!missing(`to_unit`)) {
        urlPath <- gsub(paste0("\\{", "toUnit", "\\}"), `to_unit`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PUT",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    conversions_get = function(from_unit, to_unit, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`from_unit`)) {
        queryParams['fromUnit'] <- from_unit
      }

      if (!missing(`to_unit`)) {
        queryParams['toUnit'] <- to_unit
      }

      urlPath <- "/conversions"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Conversion$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    }
  )
)
