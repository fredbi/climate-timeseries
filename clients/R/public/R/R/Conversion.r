# Climate time series API
#
# The API allows the public to search and consult time series about climate change research conducted by TheShiftProject.  The climate time series API allows contributors to upload time series about their climate change models and studies.  Other secured endpoints allows admins to maintain the nomenclatures used by the climate time series, such as units etc. Timeseries publication status and ownership is for now managed by admins. 
#
# OpenAPI spec version: v0.0.1
# Contact: fredbi@yahoo.com
# Generated by: https://github.com/swagger-api/swagger-codegen.git


#' Conversion Class
#'
#' @field toUnitCode 
#' @field factor 
#' @field intercept 
#' @field formula 
#' @field reverse_formula 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
Conversion <- R6::R6Class(
  'Conversion',
  public = list(
    `toUnitCode` = NULL,
    `factor` = NULL,
    `intercept` = NULL,
    `formula` = NULL,
    `reverse_formula` = NULL,
    initialize = function(`toUnitCode`, `factor`, `intercept`, `formula`, `reverse_formula`){
      if (!missing(`toUnitCode`)) {
        stopifnot(is.character(`toUnitCode`), length(`toUnitCode`) == 1)
        self$`toUnitCode` <- `toUnitCode`
      }
      if (!missing(`factor`)) {
        stopifnot(is.numeric(`factor`), length(`factor`) == 1)
        self$`factor` <- `factor`
      }
      if (!missing(`intercept`)) {
        stopifnot(is.numeric(`intercept`), length(`intercept`) == 1)
        self$`intercept` <- `intercept`
      }
      if (!missing(`formula`)) {
        stopifnot(is.character(`formula`), length(`formula`) == 1)
        self$`formula` <- `formula`
      }
      if (!missing(`reverse_formula`)) {
        stopifnot(is.character(`reverse_formula`), length(`reverse_formula`) == 1)
        self$`reverse_formula` <- `reverse_formula`
      }
    },
    toJSON = function() {
      ConversionObject <- list()
      if (!is.null(self$`toUnitCode`)) {
        ConversionObject[['toUnitCode']] <- self$`toUnitCode`
      }
      if (!is.null(self$`factor`)) {
        ConversionObject[['factor']] <- self$`factor`
      }
      if (!is.null(self$`intercept`)) {
        ConversionObject[['intercept']] <- self$`intercept`
      }
      if (!is.null(self$`formula`)) {
        ConversionObject[['formula']] <- self$`formula`
      }
      if (!is.null(self$`reverse_formula`)) {
        ConversionObject[['reverse_formula']] <- self$`reverse_formula`
      }

      ConversionObject
    },
    fromJSON = function(ConversionJson) {
      ConversionObject <- jsonlite::fromJSON(ConversionJson)
      if (!is.null(ConversionObject$`toUnitCode`)) {
        self$`toUnitCode` <- ConversionObject$`toUnitCode`
      }
      if (!is.null(ConversionObject$`factor`)) {
        self$`factor` <- ConversionObject$`factor`
      }
      if (!is.null(ConversionObject$`intercept`)) {
        self$`intercept` <- ConversionObject$`intercept`
      }
      if (!is.null(ConversionObject$`formula`)) {
        self$`formula` <- ConversionObject$`formula`
      }
      if (!is.null(ConversionObject$`reverse_formula`)) {
        self$`reverse_formula` <- ConversionObject$`reverse_formula`
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "toUnitCode": %s,
           "factor": %d,
           "intercept": %d,
           "formula": %s,
           "reverse_formula": %s
        }',
        self$`toUnitCode`,
        self$`factor`,
        self$`intercept`,
        self$`formula`,
        self$`reverse_formula`
      )
    },
    fromJSONString = function(ConversionJson) {
      ConversionObject <- jsonlite::fromJSON(ConversionJson)
      self$`toUnitCode` <- ConversionObject$`toUnitCode`
      self$`factor` <- ConversionObject$`factor`
      self$`intercept` <- ConversionObject$`intercept`
      self$`formula` <- ConversionObject$`formula`
      self$`reverse_formula` <- ConversionObject$`reverse_formula`
    }
  )
)
